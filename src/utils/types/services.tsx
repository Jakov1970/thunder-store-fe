export interface AuthResponseType {
  ok: boolean;
  status: number;
  message: string;
  data: any;
}

export interface OrgResponseType {
  ok: boolean;
  status: number;
  message: string;
  data: any;
}

export const blobType = "blob";
